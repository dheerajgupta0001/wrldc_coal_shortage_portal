
@model CoalShortagePortal.WebApp.Models.UserListVM
@{
    ViewData["Title"] = "Manage Users";
}


<div class="container-fluid mt-4">
    <div class="row justify-content-md-center">
        <div class="col-8">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>@ViewData["Title"]</h2>
            </div>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <p>
                    <a asp-action="Create" class="btn btn-success">
                        <i class="fas fa-plus"></i> Add New User
                    </a>
                </p>

            </div>

            @if (Model != null && Model.Users != null)
            {
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0">Existing Users</h5>
                    </div>
                    <div class="card-body">
                        @* <table class="table table-bordered table-hover"> *@
                        <table class="table table-striped table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>Username</th>
                                    <th>Email</th>
                                    <th>Phone</th>
                                    <th>Status</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var user in Model.Users)
                                {
                                    <tr>
                                        <td>@user.Username</td>
                                        <td>@user.Email</td>
                                        <td>@user.Phone</td>
                                        <td>
                                            @if (user.IsLockedOut)
                                            {
                                                <span class="badge badge-danger btn-danger">
                                                    <i class="fas fa-lock"></i> Locked
                                                </span>
                                            }
                                            else
                                            {
                                                <span class="badge badge-success btn-success">
                                                    <i class="fas fa-unlock"></i> Active
                                                </span>
                                            }
                                        </td>
                                        <td>
                                            @* <a asp-action="Edit" asp-route-id="@user.UserId">Edit</a> | *@
                                            <a asp-action="Edit" asp-route-id="@user.UserId"
                                                class="btn btn-sm btn-primary" title="Edit">
                                                <i class="fas fa-edit"></i> Edit
                                            </a>
                                            @if (user.IsLockedOut)
                                            {
                                                <form asp-action="UnlockUser" asp-route-id="@user.UserId" method="post" style="display:inline;">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-warning" onclick="return confirm('Are you sure you want to unlock this user?');">
                                                        <i class="fas fa-unlock-alt"></i> Unlock
                                                    </button>
                                                </form>
                                            }

                                            @* <a asp-action="Delete" asp-route-id="@user.UserId">Delete</a> *@
                                            <a asp-action="Delete" asp-route-id="@user.UserId"
                                                class="btn btn-sm btn-danger" title="Delete">
                                                <i class="fas fa-trash"></i> Delete
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            }

            <div class="mt-3">
                <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Back to Home
                </a>
            </div>
        </div>
    </div>
</div>


